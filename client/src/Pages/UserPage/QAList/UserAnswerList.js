import { Fragment } from 'react';

const UserAnswerList = () => {

    return (
        <Fragment>
            {/* {question && <h2>{question.filter(el => el.filter(el => el.author  === "John Doe")).length} Answers</h2>}
            {question && question.filter((el => el.answer.author === "John Doe")).map(question => 
                <UserPageListItem question={question}/>
            )} */}
        </Fragment>
    )
}

export default UserAnswerList;